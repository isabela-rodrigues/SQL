WITH PAIRS AS (
SELECT A.X AS X1, A.Y AS Y1
FROM FUNCTIONS A
INNER JOIN FUNCTIONS B
ON A.X = B.Y 
AND A.Y = B.X
WHERE A.X < A.Y

UNION

SELECT X, Y
FROM FUNCTIONS
GROUP BY X, Y
HAVING X = Y 
AND COUNT(*) > 1
)
SELECT DISTINCT X1, Y1
FROM PAIRS
ORDER BY X1;
